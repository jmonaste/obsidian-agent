"""Obsidian Research Agent.

A LangGraph agent that navigates an Obsidian vault with read-only tools
(list / glob / grep / read / backlinks / tags) in a Claude-style agent loop and
answers a query grounded in the notes, with relative-path citations.
"""

__version__ = "0.1.0"
